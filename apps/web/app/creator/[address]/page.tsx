import Link from 'next/link';
import {markets,compact} from '@/lib/domain/fixtures';
import {ArrowUpRight} from 'lucide-react';
export default async function Creator({params}:{params:Promise<{address:string}>}){const {address}=await params;if(address!=='retropick-research')return <div className="empty"><h1>Creator not found</h1><Link href="/markets">Explore markets</Link></div>;return <><div className="page-heading"><div><div className="eyebrow">CREATOR PROFILE · DEMO</div><h1>RetroPick Research</h1><p>Illustrative market collection. No verified identity or wallet address is claimed.</p></div><Link href="/create" className="btn primary">Create market</Link></div><div className="market-grid">{markets.map(m=><Link href={'/markets/'+m.id} className="market-grid-card" key={m.id}><span className="category-tag">{m.category}</span><h3>{m.question}</h3><span className="muted">{compact(m.volume)} fixture volume</span><ArrowUpRight size={15} style={{float:'right'}}/></Link>)}</div></>}
