# Development Control Plane

This directory contains implementation architecture for autonomous engineering agents.

- `launchpad/` — RetroPick Launchpad V2 implementation control plane.
- Product/protocol truth remains under `docs/`.
- Current Solidity reference remains under `contracts/docs/`.
- Completion proof remains under `evidence/`.

Development documents define HOW to build accepted product/protocol semantics. They may not silently redefine them.
