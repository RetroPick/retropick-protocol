# packages/domain Agent Contract

This package owns canonical TypeScript domain schemas/types.

No app may redefine LaunchState, Launch, Trade, QuoteAsset, Graduation, KuruMarket or domain error identifiers independently.

Onchain integer quantities use bigint internally.
