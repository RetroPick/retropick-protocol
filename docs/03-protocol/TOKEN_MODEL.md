# Token Model

Phase-1 PRISM series are ERC-20 structured claims with immutable replication definition and controlled mint/burn authority. Underlying outcome representation is abstracted by component identifiers in the math model.
