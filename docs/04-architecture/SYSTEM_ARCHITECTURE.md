# System Architecture

Phase-1 boundary:

```text
spec -> exact reference model -> proof/exhaustive tests -> contract requirements
```

Later runtime boundary:

```text
apps -> packages -> contracts
             ^
 integrations/indexers/scripts
```
