# Threat Model

See canonical failure modes in [`../protocol/FAILURE_MODES.md`](../protocol/FAILURE_MODES.md). Security review must include over-mint, double backing, replay, rounding, lifecycle and settlement funding.
