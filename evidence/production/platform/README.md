# Platform Production Evidence

Append-only evidence for shared-platform production research runs.

Do not store secrets.

Every run should identify commit, environment, configuration hash, workload, exact command, raw output and result classification.
