# Dual research status

**Date:** 2026-09-26  
**Branch:** `cursor/finance-qualification-bbd4`  
**Follow-on:** PR #3 is merged on `origin/main` at `cd457f8`. That merge contains this branch through `dd7bfb5`. Commits after that merge are the follow-on. This branch was not reset or rebased onto main.  
**Control plane:** `.agent/STATE.json` `prism.math_gate` is `FAIL` because of `CX-FP-SETTLEMENT-001`. Production Solidity remains unauthorized. This is not mainnet authorization and not a human acceptance of the new ADRs.

## Module statuses

| Module | Status |
|---|---|
| Prediction token / market | `CONTRACT_CANDIDATE` |
| PRISM | `FAIL` |
| Cross-module | `BLOCKED` |

`CONTRACT_CANDIDATE` means a research-harness kernel matches the integer model on the committed fixtures. It does not mean PRED-CONTRACT-1 PASS and it does not mean `READY_FOR_V2_INTEGRATION`.

## Gate summary

Machine-readable copy: `research/reports/qualification-gates.yaml`.

PRED-CONTRACT-1: NOT PASS.  
MATH-1: FAIL.  
MATH-1D: FAIL.  
MATH-1E: partial_z3_sympy. R-THEOREM-1, mapped to T-REPL-001, is PROVEN_UNDER_ASSUMPTIONS by a SymPy 1.14.0 identity. R-THEOREM-5 is the fifth registry row, T-BS-004, not T-ALLOC-001. SymPy 1.14.0 shows `C - QR - (S - Q)R` equals `C - SR`. Z3 5.1.0 finds that negation unsat. Runtime 0.025699s. Classification PROVEN_UNDER_ASSUMPTIONS. The per-call rule still pays 0 on the supply-2 witness and is not exact `QR`; remaining funding on that witness still holds. R-THEOREM-6 is the sixth registry row, T-ALLOC-001. Z3 5.1.0 finds the negation of the reservation invariant unsat for deposit, reserve, release, and withdraw when other series are one nonnegative total. Two reservations of 60 and 25 against balance 100 leave available 15. Reserve 60 then reserve 50 against balance 100 is rejected and the ledger is unchanged. Runtime 0.009577s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. This is not MATH-1 PASS, because the canonical per-call settlement floor still fails. T-PARTIAL-002, the partial-resolution value identity, is PROVEN_UNDER_ASSUMPTIONS. SymPy 1.14.0 reduces the post-transform gap to `B_i*(r_i-g_i)` on shapes of 1..4 components. Z3 5.1.0 finds that value equality on one state is equivalent to `B_i*g_i = B_i*r_i`, and that the negation on a remaining state is unsat. The existing model accepts component 1 paying 1 against backing 600 and 400, cash added 400, remaining states 0 and 2. State 1, outside that set, is not value-equal. A portfolio that clears the other component is not equivalent. Runtime 0.028305s. No counterexample. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-FP-001 is the ceil component-mint requirement, not a settlement-redemption claim. Z3 5.1.0 finds unsat for a ceil quotient that falls short of the numerator, and unsat for raw backing at least that ceil whose normalized value falls short of `S*x_i/WAD`. Supply 1, weight 1, 18 decimals: floor raw is 0 and does not cover the exact numerator 1; ceil raw is 1 and does. A mint with backing (1, 0) is rejected and the series stays at supply 0. A mint with backing (1, 1) succeeds and matches the requirement. Runtime 0.013474s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. `CX-FP-SETTLEMENT-001` is a different statement. MATH-1 stays FAIL. T-FP-002 is the requirement-delta component redemption on `FixedPointSeries.redeem`, not `FixedPointSettlement.redeem`. SymPy 1.14.0 shows the remaining margin equals the margin before release. Z3 5.1.0 finds unsat for a redemption that leaves backing below `Req(S-Q)` when backing started at least `Req(S)`, `0 <= Q <= S`, and the requirement is the ceil quotient. Supply 5, redeem 2, weights `WAD/2`, 18 decimals: requirement goes from 3 to 2, release is 1, remaining backing is 2. Runtime 0.022174s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-FP-003 says a guarded floor redemption preserves remaining settlement funding. It does not say the per-call floors sum to the one-shot floor. Z3 5.1.0 finds unsat for a book that starts at the ceil requirement and then falls below `Req(S-Q)` after paying `floor(Q*R/D)`. Supply 2, payout `10^18-1`, decimals 18, balance 2: the two 1-unit redemptions pay 0 and 0, the one-shot floor is 1, and sweepable dust is 2. After the first redemption the balance is 2 and the remaining requirement is 1. Funding holds. Holder fairness stays `CX-FP-SETTLEMENT-001` COUNTEREXAMPLE_FOUND. A balance of 1 against payout `10^18` and supply 2 is rejected and unchanged. Runtime 0.017595s. Classification PROVEN_UNDER_ASSUMPTIONS for the funding claim. MATH-1 stays FAIL. T-FP-004 says a minimum-backing mint followed by the inverse requirement-delta redemption has zero net component extraction. The oracle is `FixedPointSeries`, not `FixedPointSettlement`. SymPy 1.14.0 shows the deposited delta `Req(S+Q)-Req(S)` equals the release of that same quantity. Z3 5.1.0 finds unsat for a nonnegative ceil cycle whose release differs from the deposit or whose backing changes. Weights `WAD/2`, 18 decimals, mint 5 then redeem 5: both legs move 3 and 3, supply and backing return to 0. A prior surplus of 1 and 1 is still there after the cycle. Runtime 0.034207s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. The supply-2 per-call underpayment is unchanged. MATH-1 stays FAIL. T-BS-001 says an exact-backed mint preserves component backing and margin. SymPy 1.14.0 shows `(B+Qx)-(S+Q)x` equals `B-Sx`. Z3 5.1.0 finds unsat for `Q>0` and `B>=Sx` with `B+Qx < (S+Q)x`. `mint_with_exact_backing(1000)` on weights 3/5 and 2/5 leaves backing 600 and 400 and margin 0. A prior margin of 1 and 0 is unchanged by a later exact mint of 5. Runtime 0.022542s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. The bounded grid was not this identity. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-BS-002 says an exact in-kind redemption preserves remaining backing and margin. SymPy 1.14.0 shows `(B-Qx)-(S-Q)x` equals `B-Sx`. Z3 5.1.0 finds unsat for `0<Q<=S`, nonnegative weight, and `B>=Sx` with `B-Qx < (S-Q)x`. Redeeming 250 from supply 1000 on weights 3/5 and 2/5 releases 150 and 100, leaves backing 450 and 300, supply 750, and margin 0. A prior margin of 1 and 0 is unchanged by redeeming 2. Runtime 0.022627s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-BS-003 says nonnegative component backing and nonnegative component payoffs imply terminal solvency for every state. The older probe is two components and one state only. SymPy 1.14.0 reduces `V-L` to the sum of `(B_i-S*x_i)*g_i` for every finite component count. Z3 5.1.0 finds the inductive step unsat: a nonnegative partial sum plus one more nonnegative product stays nonnegative. A 3-component, 4-state exact mint of supply 4 is solvent in every state. A negative payoff is outside the assumption and can break the inequality: backing 2, supply 1, weight 1, payoff -1 gives value -2 against liability -1. Runtime 0.106505s. Classification PROVEN_UNDER_ASSUMPTIONS for every finite component count. No counterexample inside the assumption. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-NATIVE-001 says canonical binary split and merge conserve complete-set collateral. SymPy 1.14.0 shows a split or merge of `q` leaves the gaps `S_Y-S_N` and `S_Y-C` unchanged. Z3 5.1.0 finds unsat for an active split or equal merge that breaks `S_Y=S_N=C_locked` when that equality already holds. Split 100 then merge 25 leaves YES, NO, and locked collateral at 75, and open interest is 75. Merge 76 against supply 75 is rejected and the market is unchanged. Runtime 0.023312s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. Resolution payout is not this statement. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-NATIVE-002 says a valid binary terminal payoff satisfies `YES+NO=1`. Z3 5.1.0 finds unsat for a YES or NO winner whose unit payoffs do not sum to 1. A YES resolution pays 1 and 0. A NO resolution pays 0 and 1. `resolve("INVALID")` is rejected, the market stays ACTIVE, and no invalid payout is assigned. Runtime 0.007686s. Classification PROVEN_UNDER_ASSUMPTIONS for valid terminals. Invalid and void remain unspecified. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-LC-001 says the canonical PRISM lifecycle has no resurrection into issuance. Exact enumeration of `lifecycle.transition` checks all 49 ordered pairs. The seven allowed edges are DRAFT to ACTIVE, ACTIVE to MINT_PAUSED, ACTIVE to RESOLUTION_PENDING, MINT_PAUSED to RESOLUTION_PENDING, RESOLUTION_PENDING to RESOLVED, RESOLVED to REDEEMABLE, and REDEEMABLE to ARCHIVED. The other 42 pairs raise `LifecycleError`. No edge enters ACTIVE except DRAFT to ACTIVE, no edge enters DRAFT, and no non-DRAFT state reaches ACTIVE. RESOLVED and REDEEMABLE stay distinct. `cancelDraft` is not a transition. Runtime 0.000185s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-LC-002 says canonical final PRISM resolution cannot be committed twice. Exact enumeration of `PrismSeries.resolve` tries every `SeriesState` and both copies of the four terminal indices. Only `RESOLUTION_PENDING` accepts a commit, and that series accepts one. The committed state is `RESOLVED` and the payout stays at the committed value. An API series commits terminal 2 at payout 1, then rejects the repeated indices. After component 1 resolves to 1, terminals 1 and 3 do not commit, terminal 0 commits payout 2/5, and a later call does not change it. `make_redeemable` and `archive` leave the payout at 1, and resolve from `REDEEMABLE` and `ARCHIVED` is rejected. `activate`, `start_resolution`, and `resolve_component` after commitment are rejected and leave the payout unchanged. RESOLVED and REDEEMABLE stay distinct. Runtime 0.000605s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. T-PARTIAL-001 says partial-resolution NAV decomposes linearly by resolved and unresolved sets. SymPy 1.14.0 reduces the two-sum gap and the weight-linearity gap to 0 on every subset of 1..4 components, 30 partitions. Z3 5.1.0 finds unsat for nonnegative weights and values whose combined value differs from the resolved sum plus the unresolved sum, and unsat for a nonnegative weight combination that breaks linearity. `partial_resolution_nav` matches the formula on those 30 partitions. Weights 3/5 and 2/5, resolved payout 1 and mark 3/10, return 18/25. A gap or an overlap raises. A negative mark is rejected and is outside the nonnegative domain. Runtime 0.057204s. Classification PROVEN_UNDER_ASSUMPTIONS. No counterexample. This is not an exchange price. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. The 6/8/18 precision matrix has 168 cells on supplies 0, 1, 2, 2^8, 2^16, 2^32, and 2^64 and payouts 0, 1, D/2, D-1, D, D+1, 2^128, and 2^256-1. It is not a uint256 enumeration. Zero supply rejects a one-unit redemption and leaves balance 0. No sweep runs. Supply 1 pays the one-shot floor and leaves dust 0 or 1. `CX-FP-SETTLEMENT-001` reproduces at decimals 6, 8, and 18: supply 2, payout D-1, two 1-unit redemptions pay 0, the one-shot floor is 1, and dust is 2. The same underpayment appears on 48 cells. The smallest cell is supply 2, decimals 18, payout 5*10^17, paying 0 against one-shot 1. That is the same defect. The cumulative floor matches the one-shot floor on every cell and the residual stays in {0, 1}. Runtime 0.001788s. Classification EXHAUSTIVELY_VERIFIED_WITHIN_DOMAIN. No new counterexample. MATH-1 stays FAIL because the canonical per-call settlement floor still fails. The same matrix was differentially checked against `CandidateCumulativeSettlement`. 129 cells fit in uint256 and match the candidate. 15 cells are outside that domain because `supply * payout_wad` exceeds 2^256-1, including payout 2^256-1 at every supply of 2 or more. 24 zero-supply cells are outside it because the constructor reverts. `CX-FP-SETTLEMENT-001` stays a Python regression at decimals 6, 8, and 18. No Solidity function implements that per-call rule. Forge 1.8.3 and solc 0.8.26, optimizer on, 200 runs, via IR off: 1 passed, 0 failed, no mismatch. The kernel stays `differential_research_kernel`. MATH-1 stays FAIL because the canonical per-call settlement floor still fails.  
Zero-supply settlement dust: the cumulative residual under exact ceil funding is 0 or 1, and it sits unwithdrawn. Z3 5.1.0 finds unsat for a ceil-floor gap outside {0, 1}. Runtime 0.018452s. Classification of that bound: PROVEN_UNDER_ASSUMPTIONS. Sweep policy: NOT_YET_VALIDATED. A series constructed at supply 0 is accepted in Python; all 24 zero-supply cells fund at 0, reject a one-unit redemption, and leave balance 0. The Solidity constructor reverts `ZeroSupply`. After full redemption of supply 2, payout `10^18-1`, decimals 18, and funding 2, the candidate pays 0 then 1 and leaves 1. A later redeem leaves that 1 in place. Funding 5 leaves surplus 4, also unwithdrawn. `FixedPointSettlement.sweepable_dust` on the per-call witness reads 2 and does not move it. That remains `CX-FP-SETTLEMENT-001`. `PrismSeries.archive` leaves `settlement_balance` unchanged. `FixedPointSeries.sweep_dust` zeros component backing and is not a settlement withdrawal. Forge 1.8.3, solc 0.8.26, optimizer 200, via IR off: 4 passed, 0 failed. No extraction witness. The kernel stays `differential_research_kernel`. MATH-1 stays FAIL. Log: `evidence/research/prism/zero-supply-dust-2026-09-26.json`.  
Candidate settlement stateful invariants: handlers are `fund`, `makeRedeemable`, and `redeem`. `fail_on_revert` is false. Five invariants passed: redeemed supply stays within the constructed supply, `paidRaw` matches the cumulative floor, the token balance equals funding minus paid, a fully redeemed book leaves funding minus paid, and exact ceil funding leaves 0 or 1. Warmup 32 runs and depth 16 on seeds 20260926 and 20260927 passed with 512 calls and 0 reverts. The recorded campaign is 256 runs and depth 128 on the same seeds: 32768 calls, 0 reverts, 0 discards. Forge 1.8.3, solc 0.8.26, optimizer 200, via IR off. No counterexample. Sweep policy stays NOT_YET_VALIDATED. The kernel stays `differential_research_kernel`. MATH-1 stays FAIL. Log: `evidence/research/prism/candidate-settlement-invariant-2026-09-26.json`.  
Candidate backing stateful invariants: handlers are `deposit`, `mint`, and `redeem` on `CandidateComponentBacking`. `fail_on_revert` is false. Supply equals successful mints minus successful redeems. After every successful call, backing is at least the contract's `requiredRaw(supply)` on every component. When a redeem transfers tokens, the supply observed in that transfer is already the post-redeem supply. A rejected mint or redeem leaves supply and backing unchanged. Warmup 32 runs and depth 16 on seeds 20260926 and 20260927 passed with 512 calls and 0 reverts. The recorded campaign is 256 runs and depth 128: 32768 calls, 0 reverts, 0 discards. Forge 1.8.3, solc 0.8.26, optimizer 200, via IR off. No counterexample. The kernel stays `differential_research_kernel`. MATH-1 stays FAIL. Log: `evidence/research/prism/candidate-backing-invariant-2026-09-26.json`.  
Candidate reservation stateful invariants: `CandidateReservationLedger` has `deposit` and `reserve`. It has no `release` and no `withdraw`, and neither was added. `fail_on_revert` is false. For each asset, the sum of `reservedFor` equals `totalReserved`, that sum is at most `balance`, and `available` equals `balance` minus `totalReserved`. A rejected reserve leaves balance, total reserved, and the series bucket unchanged. The same warmup and the same 256 by 128 campaign on seeds 20260926 and 20260927 passed with 32768 calls and 0 reverts. No counterexample. The kernel stays `differential_research_kernel`. MATH-1 stays FAIL. Log: `evidence/research/prism/candidate-reservation-invariant-2026-09-26.json`.  
Candidate payoff-transform stateful invariants: the handler is `transformComponent` on the existing differential fixture, 4 states and backing 600 and 400. `fail_on_revert` is false. Supply stays 1000. `resolvedMask` equals the bits set by successful transforms, so a resolved component is not transformed again. A successful transform leaves the stored value unchanged on every state whose component payoff equals the submitted payout, and `payoffEquivalent` accepts the pre-transform and post-transform portfolios on the new possible mask. A rejected transform leaves cash, backing, and `resolvedMask` unchanged. Warmup 32 runs and depth 16 on seeds 20260926 and 20260927 passed with 512 calls and 0 reverts. The recorded campaign is 256 runs and depth 128: 32768 calls, 0 reverts, 0 discards, about 1.93s each. Forge 1.8.3, solc 0.8.26, optimizer 200, via IR off. No counterexample. The kernel stays `differential_research_kernel`. MATH-1 stays FAIL. Log: `evidence/research/prism/candidate-payoff-invariant-2026-09-26.json`.  
MATH-1D candidate cumulative floor: PROVEN_UNDER_ASSUMPTIONS. Domain check: EXHAUSTIVELY_VERIFIED_WITHIN_DOMAIN. Ready for ADR acceptance. Not an oracle pass.  
MATH-1B: bounded_grids_only, not a universal proof. Compositions through supply 16 on 18 decimals, two holders: 917612 states, 7340046 transitions, 8.02598s, clean. The earlier search remains 378530 states through supply 12. Backing on PrismSeries with weights {0, 1/2, 1}, supplies 0..8, quantities 0..8: 81 states, 2187 transitions, 0.054409s, clean. Canonical MATH-1 stays FAIL.  
MATH-1D candidate Solidity: `differential_research_kernel`. The kernel matches the Python fixtures. It is not MATH-1 PASS and it is not CONTRACT-1.  
Component backing Solidity: `differential_research_kernel`. `CandidateComponentBacking` matches `FixedPointSeries` deposit, mint, and requirement-delta redeem. `CandidateReservationLedger` matches the duplicate-source rejection. This is not settlement, not MATH-1 PASS, and not CONTRACT-1.  
Component backing rebase: a test-only token deposited 100 and minted supply 100 on one component with weight `10^18` and 18 decimals. Token balance, `backingRaw`, supply, and `requiredRaw` were 100. `rebaseDown` of 1, without `transfer` or `transferFrom`, left the token balance at 99. `backingRaw` stayed 100, supply stayed 100, and `requiredRaw` stayed 100. The kernel has no rebase function. Classification `COUNTEREXAMPLE_FOUND`. ADR-R08 is PROPOSED and is not accepted. The kernel was not edited. MATH-1 stays FAIL. Log: `evidence/research/prism/rebasing-component-backing-2026-09-26.json`.  
Component fee-on-transfer deposit: `transferFrom` returns true and delivers 9 of a requested 10. `deposit` reverts `Shortfall` with index 0, received 9, and expected 10. After that revert the kernel token balance is 0, `backingRaw` is 0, `requiredRaw` is 0, and supply is 0. The holder still holds 10. A following `mint(10)` reverts `InsufficientBacking` and those figures stay 0. Classification `existing_rule`. Deposit credit was not changed. MATH-1 stays FAIL. Log: `evidence/research/prism/fee-on-transfer-component-backing-2026-09-26.json`.  
Duplicate component token: the constructor accepts the same ERC-20 in both slots. One `deposit` of 10 and 10 leaves `backingRaw` at 10 and 10 and the token balance at 20. `requiredRaw(11)` is 11 and 11. `mint(11)` reverts `InsufficientBacking` with index 0, backing 10, and required 11. Supply stays 0. `mint(10)` sets supply to 10. The token balance stays 20, which covers the two requirements of 10. Classification `existing_rule`. Deposit credit was not changed. MATH-1 stays FAIL. Log: `evidence/research/prism/duplicate-component-backing-2026-09-26.json`.  
Zero component weight: the constructor accepts weights 0 and `10^18`. A deposit of 0 and 4, then `mint(4)`, leaves `backingRaw` at 0 and 4. `requiredRaw` is 0 and 4. The zero-weight token balance is 0 and the positive-weight token balance is 4. Supply is 4. The positive slot stays backed. Classification `existing_rule`. The requirement formula was not changed. MATH-1 stays FAIL. Log: `evidence/research/prism/zero-weight-component-backing-2026-09-26.json`.  
Settlement fee-on-transfer funding: supply 2, payout `10^18-1`, decimals 18, ceil funding 2. `transfer` of 2 returns true and delivers 1. The kernel stores no separate funded amount. `makeRedeemable` reads balance 1 and reverts `Underfunded` with balance 1 and required 2. `redeemable` stays false. `paidRaw` stays 0. A later `redeem(1)` reverts `NotRedeemable` and the token balance stays 1. Classification `existing_rule`. Fund, `makeRedeemable`, and `redeem` were not changed. MATH-1 stays FAIL. Log: `evidence/research/prism/fee-on-transfer-settlement-funding-2026-09-26.json`.  
Settlement rebase after redemption is open: supply 2, payout `10^18-1`, decimals 18. A normal transfer funds 2 and `makeRedeemable` succeeds. `rebaseDown` of 1 leaves the token balance at 1, `redeemable` true, and `paidRaw` 0. `redeem(2)` pays 1. That payout equals the tokens then held. `paidRaw` becomes 1 and the token balance becomes 0. The call does not revert. Classification `existing_rule`. The kernel was not edited. MATH-1 stays FAIL. Log: `evidence/research/prism/rebasing-settlement-funding-2026-09-26.json`.  
Settlement rebase below the floor: the same funded book, then `rebaseDown` of 2. The token balance is 0, below the one-shot floor of 1. `redeemable` stays true and `paidRaw` stays 0. `redeem(2)` reverts `PayoutExceedsBalance` with payout 1 and balance 0. After the revert the balance is still 0, `paidRaw` is still 0, and `redeemable` is still true. The redeem revert stays `existing_rule`. ADR-R09 is PROPOSED and is not accepted. The kernel was not edited. MATH-1 stays FAIL. Log: `evidence/research/prism/deep-rebasing-settlement-funding-2026-09-26.json`.  
Partial-resolution transform: `differential_research_kernel`. `CandidatePayoffTransform` matches the Python integers for a payoff-equivalent component conversion. Wrong component, non-equivalent payoff, and reorder were tested. It is not wired to prediction tokens. MATH-1 and CONTRACT-1 stay unmet.  
Prediction `Underfunded` and `LiveLiability`: PROVEN_UNDER_ASSUMPTIONS unreachable for numerators (2, 0), (0, 2), and (1, 1). Branch coverage stays 94.44% (34/36). That is not 100% coverage and not PRED-CONTRACT-1 PASS.  
Prediction INVALID floor compositions: ordered compositions of supply 0..16, holders A and B, labelings all-A and alternating, both cursors. Cumulative floor EXHAUSTIVELY_VERIFIED_WITHIN_DOMAIN, 131087 states, 2097154 transitions, 0.410255s. Per-call COUNTEREXAMPLE_FOUND at supply 2, parts (1, 1), payment 0 against one-shot 1. Half-up COUNTEREXAMPLE_FOUND, `half_up_both_sides(1) == 2`. `PredictionMarket._redeem` matches the cumulative floor on the same domain, 0 mismatches, 79.76s. `block_gas_limit` 600000000000 is Foundry profile `invalid_floor_compositions` only. The default profile does not set it. An isolated re-run passed 2 tests and 0 failed with the same counts in 79.63s. The default suite skips the walk when `block.gaslimit` is below that walk and still passes `test_supplyTwoPartsPayCumulativeFloor`. Profile `invalid_floor_compositions` still executes the walk: 131087 states, 2097154 transitions, 0 mismatches, 2 passed, 0 failed. PRED-MATH-1 stays partial. The kernel stays a research candidate. Log: `evidence/research/prediction/invalid-floor-compositions-2026-09-26.json`.  
MATH-1F: measured_simulation on declared synthetic books. It is not a solvency result and not Kuru liquidity. Arbitrage occurring is NOT_YET_VALIDATED.  
Minimum-cost replication: EXHAUSTIVELY_VERIFIED_WITHIN_DOMAIN for declared rational matrices of at most 16 states and 8 components. A AND B with columns A, B, and the constant 1 is PRODUCT_NOT_REPLICABLE. This is not Kuru and not a solvency theorem.  
Storage-slot isolation: INFERRED from the current kernel layouts. Two markets do not share split slots. Two series contracts do not share mint slots. Monad throughput was not measured. ADR-R06 stays PROPOSED.  
R-I08: the recorded gate `R-I08_resolved_not_redeemable` stays measured. `CandidateCumulativeSettlement.redeem` reverts `NotRedeemable` until `makeRedeemable`. The payout formula was not changed. In the INV-P numbering that test is the R-I09 funding gate. INV-P08 partial resolution already has tests.  
Invariant coverage: P-I01..P-I10 are executable. P-I05 `cancelDraft` is measured: DRAFT to ARCHIVED with `CANCELLED_BEFORE_ACTIVATION`, and collateral stays put. The immutable spec-hash branch is unchanged. The issuance invariant `invariant_preResolutionConservation` was re-run on the new bytecode. Default profile, forge 1.8.3, solc 0.8.26, optimizer 200, via IR off. Seeds 20260926 and 20260927, 256 runs, depth 500, 128000 calls, 0 discards. Reverts were 47971 and 46395. Both seeds passed. Handlers are split, merge, closeMint, and beginResolution. They do not call `cancelDraft`. PRED-CONTRACT-1 stays not_pass. Log: `evidence/research/prediction/issuance-invariant-2026-09-26.json`. R-I02 and R-I12 were prose only and now have tests in `test_invariant_ids.py`. R-I10 canonical per-call settlement stays COUNTEREXAMPLE_FOUND. X-I01..X-I07 stay not_yet_validated because the source interface is not frozen. MODULE-ADMISSION-FINANCE-1 stays not met.  
Benchmarks: `benchmarks_measured: local_single_environment`. One Foundry gas test and five-sample reference timings. Not admission. 16/16 replication solve was NOT_RUN. No percentiles.  
Static analysis: `measured_with_findings`. A complete Slither IR is `blocked_tool`. `--solc-force-legacy-json` still leaves `_redeem` without IR, and solc 0.8 rejects legacy JSON. S-P16 stays open. Inspection of the redeemed cursors is PROVEN_UNDER_ASSUMPTIONS and did not change storage. This is not PRED-CONTRACT-1 PASS.  
Fresh Slither 0.11.6 at `06366860d21402767b91578204afc1d9a1596893`: `slither . --exclude-dependencies --filter-paths openzeppelin-contracts` in `research/contract-kernels`, exit 255. `_redeem` has no IR. The error is `Function not found burn`, then `'NoneType' object has no attribute 'type'`. Classification `blocked_tool`. S-P16 stays open. The uninitialized-state detector still names `yesRedeemed` and `noRedeemed` inside `liability`. That detector text is not a new executable disagreement. The recorded legacy-JSON runs were not repeated. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. Log: `evidence/research/prediction/slither-sp16-2026-09-26.json`.  
Slither burn lookup: `OutcomeToken.burn` exists and calls `_burn(from, amount)`. `PredictionMarket` does not declare `burn`. `merge`, `burnWorthless`, and `_redeem` call `yesToken.burn` or `noToken.burn`. `split` calls `mint` and does not call `burn`. This quote does not give `_redeem` IR. Classification `blocked_tool`. S-P16 stays open. Solidity was not changed. MATH-1 stays FAIL. Log: `evidence/research/prediction/slither-burn-lookup-2026-09-26.json`.  
Compiler IR: default-profile `forge inspect PredictionMarket ir-optimized` in `research/contract-kernels`, forge 1.8.3, exit 0. The output contains `_redeem` and `burn`. Classification `compiler_sees_redeem_analyzer_does_not`. S-P16 stays open because Slither still has no IR. This is not PRED-CONTRACT-1 PASS. `foundry.toml` was not changed. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. Log: `evidence/research/prediction/compiler-redeem-ir-2026-09-26.json`.  
Foundry-targeted Slither: `slither src/prediction/PredictionMarket.sol --foundry-compile-all --exclude-dependencies --filter-paths openzeppelin-contracts` in `research/contract-kernels`, Slither 0.11.6, exit 255. `_redeem` has no IR. The error is `Function not found burn`, then `Impossible to generate IR for PredictionMarket._redeem`. Classification `blocked_tool`. S-P16 stays open. The project-wide run remains `slither-sp16-2026-09-26.json`. This is not PRED-CONTRACT-1 PASS. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. Log: `evidence/research/prediction/slither-foundry-target-2026-09-26.json`.  
Outsider burn: after `split(4)`, YES `totalSupply` is 4 and the holder balance is 4. An outsider calls `OutcomeToken.burn` for 1. The call reverts `NotMarket`. `totalSupply` stays 4 and the holder balance stays 4. Python has no `OutcomeToken`. Classification `existing_rule`. The token was not edited. No ADR was added. S-P16 stays open. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. Log: `evidence/research/prediction/outsider-burn-2026-09-26.json`.  
Outsider mint: after `split(4)`, YES `totalSupply` is 4 and the holder balance is 4. An outsider calls `OutcomeToken.mint` for 1 to the holder. The call reverts `NotMarket`. `totalSupply` stays 4 and the holder balance stays 4. Python has no `OutcomeToken`. Classification `existing_rule`. The token was not edited. No ADR was added. S-P16 stays open. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. Log: `evidence/research/prediction/outsider-mint-2026-09-26.json`.  
Zero-address initialize: `CloneableOutcomeToken.initialize(address(0), 1, "No", "NO", 18)` succeeds. `_initialized` moves from false to true. `market` is stored as the zero address. `outcomeIndex` becomes 1 and `decimals` becomes 18. `totalSupply` stays 0. The initializer writes no supply field. Python has no counterpart. Classification `recorded_finding`. No later split or redeem. ADR-P14 is PROPOSED and is not accepted. The initializer was not edited. S-P16 stays open. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. Log: `evidence/research/prediction/zero-address-initialize-2026-09-26.json`. Decision: `decisions/ADR-P14-zero-address-outcome-initialize.md`.  
Prediction market names: Solidity state-changing names are `activate`, `cancelDraft`, `split`, `merge`, `closeMint`, `beginResolution`, `resolve`, `openRedemption`, `redeemYes`, `redeemNo`, `burnWorthless`, and `archive`. Python state-changing names are `activate`, `cancel_draft`, `split`, `merge`, `close_mint`, `begin_resolution`, `resolve`, `open_redemption`, `redeem`, `burn_worthless`, and `archive`. Views and constructors are omitted. Spelling pairs cover activate, cancel, split, merge, close mint, begin resolution, resolve, open redemption, burn worthless, and archive. Unmatched names are Solidity `redeemYes` and `redeemNo`, and Python `redeem`. No function was added. ADR-P15 is PROPOSED and is not accepted. The open decision is whether Phase-1 must expose one `redeem` with a side, or two functions `redeemYes` and `redeemNo`. No winner was chosen. ADR-P11, ADR-P12, ADR-P13, and ADR-P14 stay PROPOSED. This comparison is not PRED-CONTRACT-1 PASS and not MATH-1 PASS. MATH-1 stays FAIL. S-P16 stays open. Decision: `decisions/ADR-P15-prediction-redeem-entry-points.md`.  
Python stubs: an open integer market splits 4. Collateral, YES supply, and NO supply are 4. `try_replace_spec` raises `resolution spec is immutable after activation`. `admin_mint` raises `no admin mint`. `admin_withdraw` raises `no admin withdrawal of collateral`. `pause_redemption` raises `Phase-1 has no redemption pause`. After each call those three figures stay 4. `PredictionMarket.sol` declares none of the four names. No function was added. Classification `recorded_gap`. ADR-P16 is PROPOSED and is not accepted. The open decision is whether Phase-1 must add those four operations, or they stay Python stubs that raise. No winner was chosen. ADR-P15 stays PROPOSED. MATH-1 stays FAIL. S-P16 stays open. Log: `evidence/research/prediction/python-stub-rejects-2026-09-26.json`. Decision: `decisions/ADR-P16-python-admin-stubs-raise.md`.  
NO redeem: split 4, resolve `NO_WIN`, then redeem 1 NO. Before the redeem, collateral is 4, YES supply is 4, NO supply is 4, and the NO redeemed cursor is 0. Solidity `redeemNo(1)` pays 1. After it, collateral is 3, YES supply is 4, NO supply is 3, and the NO redeemed cursor is 1. Python `redeem` of the NO side pays 1 and leaves the same four integers. Classification `existing_rule`. ADR-P15 stays a name proposal. This witness is not a payout disagreement. No ADR was added. ADR-P15 and ADR-P16 stay PROPOSED. Redeem and the payout formula were not edited. MATH-1 stays FAIL. S-P16 stays open. Log: `evidence/research/prediction/no-redeem-2026-09-26.json`.  
Losing YES redeem: the same split of 4 and the same `NO_WIN`. `burnWorthless` is not called. Before `redeemYes(1)`, collateral is 4, YES supply is 4, NO supply is 4, the YES redeemed cursor is 0, and the NO redeemed cursor is 0. The call pays 0. After it, collateral stays 4, YES supply is 3, NO supply stays 4, the YES redeemed cursor is 1, and the NO redeemed cursor stays 0. Python `redeem` of the YES side pays 0 and leaves the same integers. Classification `existing_rule`. ADR-P15 stays a name proposal. This witness is not a payout disagreement. No ADR was added. ADR-P15 and ADR-P16 stay PROPOSED. Redeem and the payout formula were not edited. MATH-1 stays FAIL. S-P16 stays open. Log: `evidence/research/prediction/losing-yes-redeem-2026-09-26.json`.  
Losing YES liability: the same split of 4, the same `NO_WIN`, and the same `redeemYes(1)` that pays 0. Before the redeem, liability is 4, collateral locked is 4, the YES redeemed cursor is 0, and the NO redeemed cursor is 0. After it, liability is 4, collateral locked is 4, the YES redeemed cursor is 1, and the NO redeemed cursor is 0. Python `redeem` of the YES side leaves the same four integers. Liability stays equal to collateral locked. Classification `existing_rule`. Liability was not edited. No ADR was added. ADR-P15 and ADR-P16 stay PROPOSED. MATH-1 stays FAIL. S-P16 stays open. Log: `evidence/research/prediction/losing-yes-liability-2026-09-26.json`.  
Source-interface comparison: `docs/prism/04-architecture/SOURCE_ASSET_INTERFACE.md` stays `proposed_not_frozen`. The interface was not frozen. The required names are `yesToken`, `noToken`, `market`, `outcomeIndex`, `decimals`, `collateralDecimals`, `resolutionSpecHash`, `collateral`, and `transfer`. `PredictionMarket` exposes `yesToken`, `noToken`, `collateralDecimals`, `resolutionSpecHash`, and `collateral`. `OutcomeToken` exposes `market`, `outcomeIndex`, `decimals`, and `transfer`. The Python model exposes none of those functions. `collateral` and `spec_hash_frozen` are attributes, and `spec_hash_frozen` is set in `activate`. The kernel hash is set in the constructor. That binding difference is already ADR-P07, which stays PROPOSED. Classification `recorded_gap`. X-I01..X-I07 stay NOT_YET_VALIDATED. No ADR was added. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. Log: `evidence/research/prism/source-interface-comparison-2026-09-26.json`.  
Deposit component walk: `CandidateComponentBacking` is constructed with two distinct tokens, weights 0 and 0, and decimals 18 and 18. One `deposit` of 1 and 1 credits `backingRaw` from 0 and 0 to 1 and 1. Each token balance on the kernel moves from 0 to 1. The holder balance moves from 1 to 0. `componentCount` stays 2. Index 2 is not readable before or after the call. Python `FixedPointSeries.deposit_raw((1, 1))` credits the same integers and does not reject. Classification `bounded_by_constructor`. Deposit was not edited. No ADR was added. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. X-I01..X-I07 stay NOT_YET_VALIDATED. Log: `evidence/research/prism/deposit-component-count-2026-09-26.json`.  
Non-depositor mint: the same weights 0 and 0 and decimals 18 and 18. Account A deposits 1 and 1. Before account B mints, supply is 0, A's component balances are 0 and 0, B's series balance is 0, and `backingRaw` is 1 and 1. Required raw at `2**256-1` is 0 and 0, so that quantity is the largest amount the backing allows. B, who did not deposit, calls `mint` for that quantity. Supply becomes `2**256-1`. `backingRaw` stays 1 and 1. A's component balances stay 0 and 0. B's series balance becomes `2**256-1`, so B received the series tokens. Python `FixedPointSeries.deposit_raw((1, 1))` then `mint(2**256-1)` reaches the same supply and backing. Python has no account and does not assign the series tokens. Classification `permissionless_mint`. Mint was not edited. No ADR was added. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. X-I01..X-I07 stay NOT_YET_VALIDATED. Log: `evidence/research/prism/non-depositor-mint-2026-09-26.json`.  
Positive-weight non-depositor mint: weights are `10^18` and `10^18`, decimals are 18 and 18. Account A deposits 1 and 1. Before account B calls `mint(1)`, supply is 0, `backingRaw` is 1 and 1, `requiredRaw` at supply 1 is 1 and 1, and B's series balance is 0. The constructor accepts these weights. B's `mint(1)` succeeds. Supply becomes 1. `backingRaw` stays 1 and 1. `requiredRaw` at supply 1 stays 1 and 1. B's series balance becomes 1, so B received the series token. Python `FixedPointSeries.deposit_raw((1, 1))` then `mint(1)` reaches the same supply, backing, and requirement. Python has no account. Classification `permissionless_mint`. This is not the zero-weight `2**256-1` mint. Mint was not edited. No ADR was added. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. X-I01..X-I07 stay NOT_YET_VALIDATED. Log: `evidence/research/prism/positive-weight-non-depositor-mint-2026-09-26.json`.  
Over-mint: the same weights `10^18` and `10^18` and decimals 18 and 18. Account A deposits 1 and 1. Before account B calls `mint(2)`, supply is 0, `backingRaw` is 1 and 1, and `requiredRaw` at supply 2 is 2 and 2. The call reverts `InsufficientBacking(0, 1, 2)`. Supply stays 0. `backingRaw` stays 1 and 1. `requiredRaw` at supply 2 stays 2 and 2. B's series balance stays 0. Python `FixedPointSeries.deposit_raw((1, 1))` then `mint(2)` raises `insufficient fixed-point backing for mint` and leaves supply 0 and backing 1 and 1. Classification `existing_rule`. Mint was not edited. No ADR was added. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. X-I01..X-I07 stay NOT_YET_VALIDATED. Log: `evidence/research/prism/over-mint-2026-09-26.json`.  
Non-depositor redeem: the same weights `10^18` and `10^18` and decimals 18 and 18. Account A deposits 1 and 1. Account B mints 1. Before B redeems 1, supply is 1, `backingRaw` is 1 and 1, `requiredRaw` at that supply is 1 and 1, A's component balances are 0 and 0, B's component balances are 0 and 0, and B's series balance is 1. After the redeem, supply is 0, `backingRaw` is 0 and 0, and `requiredRaw` at that supply is 0 and 0. A's component balances stay 0 and 0. B's component balances become 1 and 1, and B's series balance becomes 0. The component tokens sit with B, the caller. Python `deposit_raw((1, 1))`, `mint(1)`, and `redeem(1)` leave supply 0 and backing 0 and 0. Python has no accounts. Classification `redeem_pays_caller`. ADR-R10 is PROPOSED and is not accepted. Mint and redeem were not edited. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. X-I01..X-I07 stay NOT_YET_VALIDATED. Log: `evidence/research/prism/non-depositor-redeem-2026-09-26.json`. Decision: `decisions/ADR-R10-non-depositor-redeem-pays-the-caller.md`.  
Settlement payee: the existing one-holder book, supply 2, payout `10^18-1`, decimals 18, ceil funding 2. A funder who is not the series holder transfers 2. `makeRedeemable` opens the book. Before the holder redeems the full supply, the funder balance is 0, the holder balance is 0, the kernel balance is 2, `paidRaw` is 0, and `redeemable` is true. The redeem pays 1. After it, the funder balance stays 0, the holder balance is 1, the kernel balance is 1, `paidRaw` is 1, and `redeemable` stays true. Python `CumulativeFloorSettlement` has no funder account. Its paid amount is 1, and `balance_raw` goes from 2 to 1. Classification `settlement_pays_holder`. Redeem was not edited. No ADR was added. ADR-R10 stays PROPOSED. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. Log: `evidence/research/prism/settlement-pays-holder-2026-09-26.json`.  
Funder residual: the same book. After the holder redeems the full supply, the funder balance is 0, the holder balance is 1, the kernel balance is 1, and `paidRaw` is 1. The funder then calls the existing `redeem(1)`. The call reverts `InvalidQuantity(1, 0)`. Those four figures stay 0, 1, 1, and 1. The residual stays in the kernel. No sweep was added. Classification `existing_rule`. Redeem was not edited. No ADR was added. ADR-R10 stays PROPOSED. The sweep policy stays NOT_YET_VALIDATED. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. S-P16 stays open. Log: `evidence/research/prism/funder-residual-redeem-2026-09-26.json`.  
Two unit redeems: one holder, supply 2, payout `10^18-1`, decimals 18, ceil funding 2. The holder calls `redeem(1)`, then `redeem(1)`. The kernel pays 0, then 1. `paidRaw` is 0, then 1. The holder settlement-token balance is 0, then 1. The kernel balance is 2, then 1. Python `CumulativeFloorSettlement` pays 0, then 1. Classification `cumulative_floor_match`. The canonical `FixedPointSettlement.redeem` pays 0, then 0. MATH-1 stays FAIL because of that existing counterexample. ADR-R03 already records it. No new ADR was added. ADR-R10 stays PROPOSED. Redeem was not edited. The sweep policy stays NOT_YET_VALIDATED. PRED-KURU-1 stays blocked. S-P16 stays open. Log: `evidence/research/prism/two-unit-redeems-2026-09-26.json`.  
Two holders: the same book, with A and B each holding 1 series token. Fund 2 and make redeemable. A calls `redeem(1)`, then B calls `redeem(1)`. The kernel pays 0 to A and 1 to B. After A, both settlement balances are 0, the kernel balance is 2, and `paidRaw` is 0. After B, A's settlement balance stays 0, B's settlement balance is 1, the kernel balance is 1, and `paidRaw` is 1. The unit goes to the later redeem. Python `CumulativeFloorSettlement` pays 0, then 1. Classification `cumulative_floor_match`. No new ADR was added. ADR-R10 stays PROPOSED. MATH-1 stays FAIL. Redeem was not edited. The sweep policy stays NOT_YET_VALIDATED. S-P16 stays open. Log: `evidence/research/prism/two-holder-redeems-2026-09-26.json`.  
Over-reserve: `CandidateReservationLedger` deposits 100. Series A reserves 60. Before the second call, balance is 100, total reserved is 60, available is 40, series A is 60, and series B is 0. Series B then reserves 50. The call reverts `InsufficientUnreserved(asset, 50, 40)`. Those five figures stay put. Python `ReservationLedger` raises `insufficient unreserved balance for ASSET: requested=50, available=40` and leaves the same integers. Classification `existing_rule`. Reserve was not edited. No release and no withdraw were added. No ADR was added. ADR-R10 stays PROPOSED. MATH-1 stays FAIL. S-P16 stays open. Log: `evidence/research/prism/over-reserve-2026-09-26.json`.  
Remainder reserve: the same deposit of 100 and the same first reserve of 60. Before the second call, balance is 100, total reserved is 60, available is 40, series A is 60, and series B is 0. Series B then reserves 40. The call succeeds. Balance stays 100. Total reserved becomes 100. Available becomes 0. Series A stays 60. Series B becomes 40. Python `ReservationLedger` accepts and leaves the same integers. Classification `existing_rule`. The failing reserve of 50 was not repeated. Reserve was not edited. No release and no withdraw were added. No ADR was added. ADR-R10 stays PROPOSED. MATH-1 stays FAIL. S-P16 stays open. Log: `evidence/research/prism/remainder-reserve-2026-09-26.json`.  
Same-series reserve: `CandidateReservationLedger` deposits 100. Series A reserves 60. Before the second call, balance is 100, total reserved is 60, available is 40, and series A is 60. Series A then reserves 40 again. The call succeeds. Balance stays 100. Total reserved becomes 100. Available becomes 0. Series A becomes 100. Python `ReservationLedger` accepts and leaves the same integers. Classification `existing_rule`. The failing reserve of 50 and a second series reserving 40 were not repeated. Reserve was not edited. No release and no withdraw were added. No ADR was added. ADR-R10 stays PROPOSED. MATH-1 stays FAIL. S-P16 stays open. Log: `evidence/research/prism/same-series-reserve-2026-09-26.json`.  
Reservation release and withdraw: Python `ReservationLedger` state-changing names are `deposit`, `reserve`, `release`, and `withdraw`. `CandidateReservationLedger` state-changing names are `deposit` and `reserve`. The contract does not declare `release` and does not declare `withdraw`. No function was added. ADR-R11 is PROPOSED and is not accepted. The open decision is whether Phase-1 must add those two operations to the ledger, or they stay outside the kernel. No winner was chosen. T-ALLOC-001 stays the recorded Z3 proof in `evidence/research/prism/r-theorem-6-2026-09-26.json`. That proof is not MATH-1 PASS. MATH-1 stays FAIL. S-P16 stays open. Decision: `decisions/ADR-R11-python-release-and-withdraw-are-absent-from-the-ledger.md`.  
Component backing names: Python `FixedPointSeries` state-changing names are `deposit_raw`, `mint`, `mint_with_minimum_backing`, `redeem`, and `sweep_dust`. `CandidateComponentBacking` state-changing names are `deposit`, `mint`, and `redeem`. Views and constructors are omitted. Shared names are `mint` and `redeem`. Python-only names are `deposit_raw`, `mint_with_minimum_backing`, and `sweep_dust`. The Solidity-only name is `deposit`. No function was added. ADR-R12 is PROPOSED and is not accepted. The open decision is whether Phase-1 must add the one-sided names, or the kernel stays at `deposit`, `mint`, and `redeem`. No winner was chosen. ADR-R10 and ADR-R11 stay PROPOSED. This comparison is not CONTRACT-1 and not MATH-1 PASS. MATH-1 stays FAIL. S-P16 stays open. Decision: `decisions/ADR-R12-component-backing-state-changing-names.md`.  
Settlement names: `CandidateCumulativeSettlement` state-changing names are `makeRedeemable` and `redeem`. `CumulativeFloorSettlement` state-changing names are `make_redeemable` and `redeem`. `FixedPointSettlement` state-changing names are `fund`, `make_redeemable`, and `redeem`. Views and constructors are omitted. `fund` is declared only on `FixedPointSettlement`. None of the three declare a state-changing sweep. The sweep policy stays NOT_YET_VALIDATED. No function was added. ADR-R13 is PROPOSED and is not accepted. The open decision is whether Phase-1 must add `fund` to the cumulative kernel and `CumulativeFloorSettlement`, or `fund` stays only on `FixedPointSettlement`. No winner was chosen. The per-call floor failure stays ADR-R03. This comparison is not CONTRACT-1 and not MATH-1 PASS. MATH-1 stays FAIL. S-P16 stays open. Decision: `decisions/ADR-R13-settlement-fund-is-only-on-fixed-point-settlement.md`.  
Unredeemed liability: after `split(4)` and before any redeem, `yesRedeemed` is 0, `noRedeemed` is 0, collateral locked is 4, and liability is 4 on both sides. After `YES_WIN` and redeeming 1 YES, those figures are 1, 0, 3, and 3 on both sides. The payout is 1. Classification `existing_rule`. Liability and redeem were not edited. No ADR was added. S-P16 stays open. MATH-1 stays FAIL. PRED-KURU-1 stays blocked. X-I01..X-I07 stay NOT_YET_VALIDATED. Log: `evidence/research/prediction/unredeemed-liability-2026-09-26.json`.  
Local reproducibility: `rerun_pass`. Rerun of `research/benchmarks/scripts/repro_local.sh` at `0f14c30b1143067e051693fc9c0633683a937ce6`, Python 3.12.3, forge 1.8.3 (`cae51ad458f6abb64852b7709eb784352429825d`). Prediction unit tests 15 OK. PRISM unit tests 109 OK. Default-profile `forge test`: 76 passed, 0 failed, 1 skipped (77 total). The skip is `test_invalidCompositionsMatchCumulativeFloor` because `block.gaslimit` is below the composition walk. `test_supplyTwoPartsPayCumulativeFloor` passed. This command did not re-execute the supply 0..16 domain. Existing interpreter and existing Foundry install. This is not a clean clone and not a fresh virtualenv. The same forge command ran `invariant_preResolutionConservation` without a named seed (256 runs, 128000 calls, 46825 reverts, pass). That count belongs to this command and leaves the seeds 20260926 and 20260927 at 47971 and 46395. Log: `evidence/research/repro/repro-local-2026-09-26-rerun.txt`. The earlier log stays `evidence/research/repro/repro-local-2026-09-26.txt`.  
Contradiction 2 stays open. Classification `recorded_contradiction`. ADR-P10 is PROPOSED and is not accepted. `native_market.py` stays the reduced oracle. Neither redeem function was edited. Log: `evidence/research/prediction/resolved-redeem-contradiction-2026-09-26.json`.  
False-return collateral stays open. `create_market` with `CollateralClass.FALSE_RETURN` raises `Phase-1 admits only standard ERC-20 collateral, got FALSE_RETURN` and does not call transfer. The Solidity kernel constructs and activates the market. `transfer` and `transferFrom` return false. `split(1)` reverts `SafeERC20FailedOperation`. `collateralLocked`, `yesSupply`, and `noSupply` stay 0. Classification `recorded_contradiction`. ADR-P08 is PROPOSED. Acceptance is not granted. The kernel was not edited. MATH-1 stays FAIL. PRED-MATH-1 stays partial. PRED-CONTRACT-1 stays not_pass. Log: `evidence/research/prediction/false-return-collateral-2026-09-26.json`.  
Rebasing collateral: `create_market` with `CollateralClass.REBASING` raises `Phase-1 admits only standard ERC-20 collateral, got REBASING`. The kernel has no rebase function. A test-only token ran `split(100)`, then `rebaseDown` of 1 without `transfer` or `transferFrom`. Collateral balance went from 100 to 99. `collateralLocked`, YES supply, and NO supply stayed 100. `liability()` stayed 100. Classification `COUNTEREXAMPLE_FOUND`. ADR-P06 stays PROPOSED. ADR-P09 is PROPOSED and is not accepted. The kernel was not edited. Log: `evidence/research/prediction/rebasing-collateral-2026-09-26.json`.  
Split callback: Python rejects `CollateralClass.ERC777` at construction and has no token callback. A nested `split` hook raises `reentrancy` and leaves `collateral_locked`, YES supply, and NO supply at 0. The kernel guard is the existing `nonReentrant` modifier. A test-only token called `split(1)` from `transferFrom` during `split(100)`. The inner call reverted `ReentrancyGuardReentrantCall`, and that revert is the outer `split` revert. Collateral balance, `collateralLocked`, YES supply, and NO supply stayed 0. Classification `existing_guard`. Log: `evidence/research/prediction/split-callback-2026-09-26.json`.  
Live collateral: after `split(4)`, factory, resolver, and an arbitrary address called `cancelDraft`, `archive`, `redeemYes`, `redeemNo`, and `merge`. Each revert left collateral balance, `collateralLocked`, YES supply, and NO supply at 4. The holder `merge(1)` burned both sides first and left 3, 3, 3, and 3. After YES resolution, `archive` and a redeem without tokens left 3, 3, 3, and 3. Holder `redeemYes(1)` burned YES first and left balance 2, `collateralLocked` 2, YES 2, and NO 3. Python `admin_withdraw` raises and leaves 4, 4, and 4. The kernel has no withdraw function. Classification `existing_rule`. Log: `evidence/research/prediction/live-collateral-withdrawal-2026-09-26.json`.  
Double YES redeem: after `split(4)`, a YES resolution, and open redemption, the same holder redeems YES 4 twice. Python pays 4 on the first call and leaves `collateral_locked` 0, YES supply 0, holder YES 0, and NO supply 4. The second call raises `redeem exceeds balance` and those figures stay put. Solidity `redeemYes(4)` pays 4 and leaves collateral balance 0, `collateralLocked` 0, YES supply 0, holder YES 0, and NO supply 4. The second `redeemYes(4)` reverts `ERC20InsufficientBalance` and those figures stay put. The holder is not paid twice. Classification `existing_rule`. Redeem and resolve were not changed. Log: `evidence/research/prediction/double-yes-redeem-2026-09-26.json`.  
Second YES resolution: after `split(4)` and the first `YES_WIN`, the resolver commits `NO_WIN`. Python raises `illegal transition RESOLVED -> RESOLVED`. Solidity reverts `BadState`. On both sides the payout stays numerators 2 and 0, collateral stays 4, YES supply stays 4, and NO supply stays 4. Classification `existing_rule`. Resolve was not changed. Log: `evidence/research/prediction/second-yes-resolution-2026-09-26.json`.  
Unequal YES and NO merge: while the market is OPEN, a holder with YES 1 and NO 4 calls `merge(4)`. Python raises `merge requires equal YES and NO balances` and does not return. Solidity reverts `ERC20InsufficientBalance` on the short YES burn, balance 1 needed 4. Collateral stays 4, YES supply stays 4, and NO supply stays 4. Holder balances stay YES 1 and NO 4. Classification `existing_rule`. Split and merge were not changed. MATH-1 stays FAIL. Log: `evidence/research/prediction/unequal-merge-2026-09-26.json`.  
Uint256 split: Python can represent `2**256-1`. `split` of that amount leaves collateral, YES supply, and NO supply at `2**256-1`. A following `split(1)` succeeds and those three figures become `2**256`. Solidity `split(type(uint256).max)` leaves the same three figures at `2**256-1`. A further `split(1)` reverts `Panic(0x11)`. After the revert, collateral, YES supply, and NO supply stay at `2**256-1`. The supply does not wrap. Classification `recorded_contradiction`. ADR-P11 is PROPOSED and is not accepted. Split was not changed. MATH-1 stays FAIL. PRED-MATH-1 stays partial. PRED-CONTRACT-1 stays not_pass. Log: `evidence/research/prediction/uint256-split-2026-09-26.json`.  
Split max: after `split(10)`, a single `split(2**256)` raises `amount exceeds configured maximum`. Collateral, YES supply, and NO supply stay 10. The state stays `OPEN`. Solidity cannot pass `2**256` as a `uint256` argument, so the second split is not called and those three figures stay 10. Classification `existing_rule`. Split was not changed. MATH-1 stays FAIL. Log: `evidence/research/prediction/split-max-2026-09-26.json`.  
Configured split maximum: Python with `max_amount` 1000000000 rejects `split(1000000001)` and raises `amount exceeds configured maximum`. Collateral, YES supply, and NO supply stay 0. Solidity has no such cap. `split(1000000001)` mints. Those three figures become 1000000001. Classification `recorded_contradiction`. ADR-P12 is PROPOSED and is not accepted. Split was not changed. The configured maximum was not changed. MATH-1 stays FAIL. PRED-MATH-1 stays partial. PRED-CONTRACT-1 stays not_pass. Log: `evidence/research/prediction/configured-split-maximum-2026-09-26.json`.  
Invalid NO burn: after `split(5)`, INVALID, and five YES redemptions of 1 that pay 0, 1, 0, 1, and 0, collateral is 3, YES supply is 0, and NO supply is 5. `burn` of NO 1 raises `side is not worthless` in Python and reverts `NotWorthless` in Solidity. Those figures stay put. The holder still has NO 5. Classification `existing_rule`. Burn was not changed. MATH-1 stays FAIL. Log: `evidence/research/prediction/invalid-burn-rejected-2026-09-26.json`.  
Repeat activate: after the market is OPEN, a second `activate` reverts. Python raises `activate only from DRAFT`. Solidity reverts `BadState`. Collateral, YES supply, and NO supply stay 0. Holder YES and NO stay 0. The state stays OPEN. Classification `existing_rule`. Activate was not changed. MATH-1 stays FAIL. Log: `evidence/research/prediction/repeat-activate-2026-09-26.json`.  
Draft split: while the market is DRAFT, `split(1)` with a full collateral delivery reverts. Python raises `split only while OPEN`. Solidity reverts `BadState`. Collateral, YES supply, and NO supply stay 0. Holder YES and NO stay 0. The state stays DRAFT. Classification `existing_rule`. Split was not changed. MATH-1 stays FAIL. Log: `evidence/research/prediction/draft-split-2026-09-26.json`.  
Rejection inventory: 72 remaining public calls that both sides can execute where a reject is expected. 71 rows reject on both sides and leave collateral, YES supply, and NO supply unchanged, with the state unchanged. `archive` from `DRAFT` raises `P-I05 resolution spec mutated or missing` in Python after the state is already `ARCHIVED`. Collateral, YES supply, and NO supply stay 0. Solidity reverts `BadState` and the state stays `DRAFT`. Those three figures stay 0. Classification of that row is `recorded_contradiction`. ADR-P13 is PROPOSED and is not accepted. Archive was not changed. MATH-1 stays FAIL. Log: `evidence/research/prediction/rejection-inventory-2026-09-26.json`.  
MODULE-ADMISSION-FINANCE-1: not met.  
PRISM CONTRACT-ARCH-1: proposed, contingent on ADR-R03. Not a pass.  
SOURCE-ASSET-INTERFACE-FREEZE: proposed_not_frozen.  
PRED-KURU-1: blocked.  
Kuru backing claim: absent. No research kernel and no Python model reads Kuru orderbook liquidity as protocol backing or as redemption value. Prediction liability is locked collateral. PRISM component redeem releases the drop in `backingRaw`. `market_microstructure.py` sets `not_kuru_liquidity` true. PRED-KURU-1 stays blocked. MATH-1 stays FAIL. Log: `evidence/research/prediction/kuru-backing-claim-2026-09-26.json`.

## What changed since the baseline

The baseline file remains the pre-change record. After it:

- a prediction reference model and Foundry kernel were added outside `contracts/src/v2`;
- the PRISM suite was re-run and a settlement counterexample was locked in as a test;
- proposed ADRs P01–P07 and R01–R07 were added and labeled PROPOSED.

## Contradictions still open

1. Canonical diagrams name `CompleteSetVault`. ADR-P03 proposes not to deploy one. The diagram was not rewritten as if the ADR were accepted.
2. `research/prism-model/native_market.py` still collapses RESOLVED and REDEEMABLE. `BinaryCompleteSetMarket.redeem("YES", 1)` after `split(4)` and `resolve("YES")` pays 1 while the state stays `RESOLVED`. Collateral moves from 4 to 3. The enum is ACTIVE, RESOLVED, ARCHIVED. `PredictionMarket.redeem` on a YES_WIN market in `RESOLVED` raises `redeem only while REDEEMABLE`. Collateral stays 4 and `yes_redeemed` stays 0. Classification: recorded_contradiction. The contradiction stays open. This is not a theorem pass. MATH-1 stays FAIL. PRED-MATH-1 stays partial. PRED-CONTRACT-1 stays not_pass. ADR-P10 is PROPOSED and is not accepted. Neither redeem function was edited. Log: `evidence/research/prediction/resolved-redeem-contradiction-2026-09-26.json`.
3. Evidence note from 2026-09-17 says 51 tests. Baseline discovery saw 55. Later discovery saw 60 after new tests.
4. Launchpad `status.yaml` still says the web app is unimplemented. That lane was not edited.
5. `.agent/STATE.json` previously said MATH-1 `IN_PROGRESS`. It now says `FAIL` for the settlement rule. Accepted ADR-002 is unchanged.
6. A collateral token whose `transfer` returns false is rejected at Python construction and, in the kernel, only when `split` calls `transferFrom`. The market still constructs and activates. Classification: recorded_contradiction. The contradiction stays open. ADR-P08 is PROPOSED and is not accepted. Log: `evidence/research/prediction/false-return-collateral-2026-09-26.json`.
7. Python `split(2**256-1)` then `split(1)` stores collateral, YES supply, and NO supply at `2**256`. Solidity `split(type(uint256).max)` then `split(1)` reverts `Panic(0x11)` and leaves those figures at `2**256-1`. Classification: recorded_contradiction. The contradiction stays open. ADR-P11 is PROPOSED and is not accepted. Split was not edited. MATH-1 stays FAIL. PRED-MATH-1 stays partial. PRED-CONTRACT-1 stays not_pass. Log: `evidence/research/prediction/uint256-split-2026-09-26.json`.
8. Python with `max_amount` 1000000000 rejects `split(1000000001)` and leaves collateral, YES supply, and NO supply at 0. Solidity `split(1000000001)` mints and those figures become 1000000001. Classification: recorded_contradiction. The contradiction stays open. ADR-P12 is PROPOSED and is not accepted. Split was not edited. MATH-1 stays FAIL. PRED-MATH-1 stays partial. PRED-CONTRACT-1 stays not_pass. Log: `evidence/research/prediction/configured-split-maximum-2026-09-26.json`.
9. Python `archive` from `DRAFT` raises `P-I05 resolution spec mutated or missing` after the state is `ARCHIVED`. Collateral, YES supply, and NO supply stay 0. Solidity `archive` reverts `BadState` and the state stays `DRAFT`. Those figures stay 0. Classification: recorded_contradiction. The contradiction stays open. ADR-P13 is PROPOSED and is not accepted. Archive was not edited. MATH-1 stays FAIL. PRED-MATH-1 stays partial. PRED-CONTRACT-1 stays not_pass. Log: `evidence/research/prediction/rejection-inventory-2026-09-26.json`.

## Blockers

- Human acceptance of ADR-P01..P16, of ADR-R03's cumulative-floor repair, of ADR-R08, of ADR-R09, of ADR-R10, of ADR-R11, of ADR-R12, and of ADR-R13. The candidate bound is recorded. Acceptance was not granted. ADR-P08, ADR-P09, ADR-P10, ADR-P11, ADR-P12, ADR-P13, ADR-P14, ADR-P15, ADR-P16, ADR-R08, ADR-R09, ADR-R10, ADR-R11, ADR-R12, and ADR-R13 do not edit the kernel.
- Human acceptance before any production settlement port. A candidate kernel now exists under `research/contract-kernels/src/prism/`. CONTRACT-1 stays not_met. ADR-R07 still stops a v2 port.
- Complete Slither IR or another static-analysis pass. `PredictionMarket.sol` branch coverage was remeasured at 94.44% (34/36). `Underfunded` and `LiveLiability` remain uncovered. That is not a pass by itself.
- Kuru router address and a RetroPick parameter set. The worksheet's book rows are BLOCKED after a second primary-source pass. No fork and no deployment.
- Cross-module differential harness. The source-asset list is `proposed_not_frozen`, not frozen. X-I01..X-I07 are NOT_YET_VALIDATED. Blocker: source interface not frozen.
- Kernel `cancelDraft` for the cancelled-draft branch of P-I05 is measured. Remaining PRED-CONTRACT-1 blockers are Kuru, Slither IR, and resolver trust.
- Echidna, Medusa, Halmos, Mythril, semgrep: not installed. solhint 5.2.0 ran locally and is not a clean-audit substitute. Slither's focused rerun still has incomplete IR.
